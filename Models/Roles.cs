namespace GiftoftheGivers.Models
{
    // Central place for the two required roles so we never typo a role string.
    public static class Roles
    {
        public const string Employee = "Employee";
        public const string Donor = "Donor";
    }
}